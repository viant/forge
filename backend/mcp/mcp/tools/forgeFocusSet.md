# forgeFocusSet

Focus a specific control by `{windowId, dataSourceRef, controlId}`.

Maps to UI command: `ui.focus.set`.

Note: requires the frontend control registry (auto-registered via `ControlWrapper`).

