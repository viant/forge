# forgeControlsSearch

Search registered UI controls by substring match over `controlId`, `label`, `type`, `scope`, `dataSourceRef`, `windowId`.

Maps to UI command: `ui.controls.search`.

## Input

```json
{
  "query": "status",
  "windowId": "optional-windowId",
  "limit": 50
}
```

