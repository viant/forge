# forgeFocusGet

Return the currently focused control (if any) from the UI registry.

Maps to UI command: `ui.focus.get`.

## Output

```json
{ "focused": { "windowId":"...", "dataSourceRef":"...", "controlId":"...", "label":"..." } }
```

